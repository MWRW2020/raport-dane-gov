name: 🇵🇱 Generowanie XML i publikacja na GitHub Pages

on:
  push:
    branches:
      - main
  schedule:
    # Uruchomienie o północy (00:00 UTC) każdego dnia
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  generate_and_publish:
    runs-on: ubuntu-latest
    
    # Ustawienie uprawnień do repozytorium
    permissions:
      contents: write 
      pages: write    
      id-token: write 

    steps:
      - name: 1. Checkout repository (Read/Write)
        uses: actions/checkout@v4

      - name: 2. Konfiguracja środowiska Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: 2.5. Czyszczenie pliku z niewidocznych znaków (FIX)
        # Usuwa znaki, które mogły trafić do pliku podczas kopiowania i powodują błąd 'TypeError'
        run: |
          echo "Czyszczenie pliku generator_danych_gov.py z ukrytych znaków..."
          # Usuń znaki niełamiącej spacji (NBSP)
          sed -i 's/[\xC2\xA0]//g' generator_danych_gov.py
          # Usuń znaki powrotu karetki (CR)
          sed -i 's/[\r]//g' generator_danych_gov.py
          echo "Plik został wyczyszczony."

      - name: 3. Uruchomienie skryptu generującego XML i MD5
        run: python generator_danych_gov.py

      - name: 4. Konfiguracja GitHub Pages
        uses: actions/configure-pages@v5

      - name: 5. Wgranie artefaktów do Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: './'

      - name: 6. Publikacja na GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
